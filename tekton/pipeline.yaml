apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: tax-calculator-pipeline
spec:
  tasks:
    - name: npminstall
      taskRef:
        name: npm-build-task
    - name: tests
      runAfter:
        - npminstall
      taskRef:
        name: jasmine-test-task
    - name: build
      runAfter:
        - tests
      taskRef:
        name: npm-build-task
